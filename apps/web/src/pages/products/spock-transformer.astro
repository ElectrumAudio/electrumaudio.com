---
import ProductsLayout from '../../layout/ProductsLayout.astro';
// import * as product from '../../products/spock-transformer.md';
const pageTitle = "Electrum Audio | Products | Spock Transformer";
const pieces = await Astro.glob('../../products/spock-transformer/*.md');
---
<ProductsLayout title={pageTitle}>
    {pieces.map(piece => (
        <piece.Content />
    ))}
</ProductsLayout>